<?php
    $connection = mysqli_connect(
        $hostname = "localhost",
        $username = "root",
        $password = "",
        $database = "tasks-app"
    );
    //if($connection){echo "Database connected";}
?>